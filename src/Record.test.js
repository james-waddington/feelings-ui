import { render, screen } from '@testing-library/react';
import App from './App';

test.todo('Saves the feeling when the save button is clicked');

test.todo('Changes the value when a value button is clicked');

test.todo('Saves the tags when tags are added');
